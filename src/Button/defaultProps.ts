import { ButtonProps } from './type';

export const buttonDefaultProps: ButtonProps = {
  type: 'default',
  size: 'middle',
  nativeType: 'button',
  block: false,
  loading: false,
  ghost: false,
};
